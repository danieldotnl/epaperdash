{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run dashboard server (dev)",
      "type": "shell",
      "command": "uv run uvicorn server:app --host 0.0.0.0 --port 8000 --reload",
      "options": {
        "cwd": "${workspaceFolder}/epaperdash"
      },
      "isBackground": true,
      "presentation": {
        "reveal": "always",
        "panel": "dedicated"
      },
      "problemMatcher": []
    }
  ]
}
