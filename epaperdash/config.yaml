name: ePaperDash
version: "0.1.5"
slug: epaperdash
description: Renders a dashboard image for an e-paper panel
arch:
  - amd64
init: false
ports:
  8000/tcp: 8000
ports_description:
  8000/tcp: Dashboard image endpoint
